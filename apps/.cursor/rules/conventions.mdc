---
description: Naming conventions and coding style guidelines
globs: **/*.ts,**/*.svelte
alwaysApply: true
---

# Naming Conventions

## Case Conventions

- **camelCase**: variables, functions, methods
- **PascalCase**: classes, interfaces, types, components
- **SCREAMING_SNAKE_CASE**: constants and environment variables
- **snake_case**: analytics events and database columns

## Examples

```typescript
// ✅ Good
const eventId = 'event-123';
class EventService { }
const MAX_EVENTS = 100;
export const event = sqliteTable('event', {
  church_id: text('church_id'),
  week_number: integer('week_number')
});
statsigService.logEvent('admin_misa_view', 'load', session);

// ❌ Bad
const EventId = 'event-123';
class eventService { }
const maxEvents = 100;
churchId: text('churchId'); // Wrong casing
statsigService.logEvent('adminMisaView', 'load', session);
```

## Descriptive Names

```typescript
// ✅ Good
async function retrieveEventsByWeekRange(options: WeekRangeOptions): Promise<ChurchEvent[]> { }
const isSubmitting = $state(false);
const hasEvents = events.length > 0;

// ❌ Bad
async function getEvts(opts: any): Promise<any[]> { }
const sub = $state(false);
const events = events.length > 0; // Confusing
```

## File Naming

- Files match exported class/interface name (PascalCase for classes, camelCase for utilities)
- Test files: `*.test.ts` or `*.spec.ts`

## Logging Format

Format: `file_name + method name`

```typescript
logger.error('EventService.createEvent: Failed to insert event', { eventId, error });
```

## Constants vs Functions

Prefer constants for static values:

```typescript
// ✅ Good
const DEFAULT_WEEK_NUMBER = 1;

// ❌ Bad
function getDefaultWeekNumber(): number { return 1; }
```

## Boolean Naming

Use prefixes: `is`, `has`, `should`, `can`

```typescript
const isSubmitting = $state(false);
const hasEvents = events.length > 0;
const canEdit = user.role === 'admin';
```
